<% content_for :page_title, "#{@timeline.title} - 出来事登録" %>
<% content_for :header_label, "ADD" %>

<section class="section">
  <h2 class="section-title">出来事登録</h2>
  <%= render "form", timeline: @timeline, event: @event %>
</section>
