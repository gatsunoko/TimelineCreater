<% content_for :page_title, "#{@timeline.title} - 設定" %>
<% content_for :header_label, "SETTINGS" %>

<section class="section">
  <h2 class="section-title">年表設定</h2>
  <%= render "form", timeline: @timeline %>
</section>
